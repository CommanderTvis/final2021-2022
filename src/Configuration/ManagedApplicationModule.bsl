// @strict-types
Процедура ПередНачаломРаботыСистемы(Отказ)
	НуженРежимРабочегоОкна = Ложь;
	НастройкаИнтерфейсаВызовСервера.НастройкаИнтерфейса(Отказ, НуженРежимРабочегоОкна);
	Если НуженРежимРабочегоОкна Тогда
		КлиентскоеПриложение.УстановитьРежимОсновногоОкна(РежимОсновногоОкнаКлиентскогоПриложения.РабочееМесто);
	КонецЕсли;
КонецПроцедуры