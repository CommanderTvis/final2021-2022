// @strict-types
&НаКлиенте
Процедура ИнициаторПриИзменении(Элемент)
	ФИОИнициатора = Объект.Инициатор.Фамилия + " " + Объект.Инициатор.Имя + " " + Объект.Инициатор.Отчество;
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если ЗначениеЗаполнено(Объект.Инициатор) Тогда
		ФИОИнициатора = Объект.Инициатор.Фамилия + " " + Объект.Инициатор.Имя + " " + Объект.Инициатор.Отчество;
	КонецЕсли;
КонецПроцедуры