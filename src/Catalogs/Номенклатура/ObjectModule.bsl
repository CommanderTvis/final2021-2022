//@strict-types

Процедура ПередЗаписью(Отказ)
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;

	Если Не ПроверкаУникальностиВызовСервера.ПрошелКонтрольУникальностиСправочникаПоРеквизиту(
		Новый Структура("Наименование", Наименование), Ссылка) Тогда
		СообщенияКлиентСервер.СообщитьСоСвойствами("Наименование должна быть уникальным.", "Наименование", ЭтотОбъект);
		Отказ = Истина;
	КонецЕсли;
КонецПроцедуры